package internal

const (
	// ProviderOpenStack is the name of the OpenStack provider.
	ProviderOpenStack = "openstack"
	// IngressNodeLabelDefault is the default label for ingress nodes.
	IngressNodeLabelDefault = "node-role.kubernetes.io/ingress"
	// TrueString is the string representation of true.
	TrueString = "true"
	// ARecord is the string representation of an A record.
	ARecord = "A"
	// CernChSuffix is the suffix for cern.ch domains.
	CernChSuffix = "cern.ch"
	// CernChDomain is the domain for cern.ch.
	CernChDomain = ".cern.ch"
	// LandbAliasPrefix is the prefix for landb-alias metadata keys.
	LandbAliasPrefix = "landb-alias"
	// AuthFailed is the error message for authentication failure.
	AuthFailed = "Authentication failed"
	// Unauthorized is the error message for unauthorized access.
	Unauthorized = "unauthorized"
)
